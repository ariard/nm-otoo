nm mach-o
otool mach-o

bonus:
nm options
nm elf

files : executable, object code, shared libraries, dynamic loaded-libraries, core dumps


TODO : 

script for test each bin against each bin and if failure print it

- all types of symbols (undefined, absolute, text section symbol, data section symbol, bss section symbol, common symbol, debugger symbol table, other, indirect symbol) local/global

A + I to be found in /usr/lib

- check endianess on fat file
- check stab 5 (/usr/bin/Rez) => print the full entry | align stabs

+ hardness

SAME on otool

+ bonus : 10 nm + (A)  + otool (h, f, d, a) + SYMDEF ?

exe : /bin/ + /usr/bin + /usr/bin/ + /sbin + /usr/local/bin

.o / .so / .dll / .a

TODO :
	- screener code
	- cleaner repo
	- ajouter DYLD (shared libraries)
	- tester contre correction partie obligatoire


LESSONS :
	- lib debug project
