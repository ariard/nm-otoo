nm mach-o
otool mach-o

bonus:
nm options
nm elf

files : executable, object code, shared libraries, dynamic loaded-libraries, core dumps

- check DYSYMTAB
- check stab 5
- do add multiple args
- check value on otool bash

TODO : 

- all types of files (exe, obj, so, dll, core dump)
- 64 bit/ 32 bit/fat file (/usr/bin/audiofile/ ; my_32_bit )
- all types of symbols (undefined, absolute, text section symbol, data section symbol, bss section symbol, common symbol, debugger symbol table, other, indirect symbol) local/global

+ hardness

+ bonus : 8nm + 2 otool


.o / .so / .dll / .a

QUESTIONS :
	- support 32 bits ?
	- support fat file ?
	- support archive/object file

TODO :
	- screener code
	- cleaner repo
	- ajouter DYLD (shared libraries)
	- tester contre correction partie obligatoire


LESSONS :
	- lib debug project
